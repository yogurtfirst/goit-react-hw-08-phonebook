import { SignInForm } from '../../components/SignInForm/SignInForm';

const SignInPage = () => {
    return (
        <main>
            <SignInForm />
        </main>
    );
};

export default SignInPage;